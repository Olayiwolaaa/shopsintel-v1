{
  "builds": [
    {
      "src": "src/app.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/app.py"
    }
  ],
  "buildCommand": "pip install -r requirements.txt && prisma db push --schema=prisma/schema.prisma && uvicorn app:app --host 0.0.0.0 --port 8000"
}
